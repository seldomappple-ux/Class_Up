# docs

这里放项目长期文档。

- `requirements/`: 需求、用户故事、验收标准
- `design/`: 架构设计、模块设计、重要取舍
- `api/`: 接口契约、数据格式、对外协议
- `数据协议.md`: 配置、manifest、输出目录、清理审计等主数据契约
经验沉淀统一放在 `.agents/progress/entries/`，不要在 `docs/` 下另建沉淀文档。
